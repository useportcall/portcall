# Local development only — do NOT use these values in production.

REDIS_ADDR="127.0.0.1:6379"

# Email provider: "local" (mailpit), "postmark", or "resend"
EMAIL_PROVIDER=local
SMTP_SERVER=localhost:1025
EMAIL_FROM=dev@localhost

# Uncomment for Postmark:
# EMAIL_PROVIDER=postmark
# POSTMARK_API_KEY=your-postmark-api-key-here

# Uncomment for Resend:
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=replace-with-your-resend-api-key
